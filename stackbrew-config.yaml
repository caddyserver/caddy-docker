caddy_version: v2.0.0-beta.20
caddy_filename_prefix: caddy2_beta20
# sha256 checksums for github-released binaries
checksums:
  amd64: a9a9476593a3cc0c0c4d72cb9cf538c1fddf5fc2161b8e47fe16116da72b6d1b
  arm64v8: 4e3b82865da7fdd504cd74373a0d2aab39c4cce23613498cd9c0e067aad38107
# configuration for the stackbrew.tmpl template
variants:
  - dir: alpine
    tags: [ "{{.conf.caddy_version | strings.TrimPrefix `v` }}", "{{.conf.caddy_version | strings.TrimPrefix `v` }}-alpine", "alpine", "latest" ]
    architectures: [ amd64 ]
  # Drop this for now!
  # - dir: scratch
  #   tags: [ "{{.conf.caddy_version}}-slim", "scratch" ]
  #   architectures: [ amd64 ]
  - dir: builder
    tags: [ "{{.conf.caddy_version | strings.TrimPrefix `v` }}-builder", "builder" ]
    architectures: [ amd64 ]
